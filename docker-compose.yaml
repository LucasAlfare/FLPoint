services:
  fl-points:
    container_name: fl-points-container
    build: .
    restart: unless-stopped

    ports:
      - "7171:7171"

    environment:
      PASS: pass # just ignore, dummy
#      APP_ENV: prod
#      DATABASE_JDBC_URL: jdbc:sqlite:/data/data.db
#      DATABASE_JDBC_CLASS_NAME: org.sqlite.JDBC
#      DATABASE_USERNAME: ""
#      DATABASE_PASSWORD: ""
#      WEBSERVER_PORT: 7171
#      JWT_ALGORITHM_SIGN_SECRET: super-secret-production

    volumes:
      - ./data:/data